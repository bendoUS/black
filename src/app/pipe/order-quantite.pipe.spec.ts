import { OrderQuantitePipe } from './order-quantite.pipe';

describe('OrderQuantitePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderQuantitePipe();
    expect(pipe).toBeTruthy();
  });
});
