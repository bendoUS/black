import { OrderAlphaPipe } from './order-alpha.pipe';

describe('OrderAlphaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderAlphaPipe();
    expect(pipe).toBeTruthy();
  });
});
